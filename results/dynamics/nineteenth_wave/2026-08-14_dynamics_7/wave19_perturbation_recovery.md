# Wave-19 proposal perturbation recovery

The frozen `sigma ∈ {0.05, 0.10, 0.20}` proposal-recovery experiment was not run because the representation
R-gate failed. No noisy F1 proposal or matched refinement control was evaluated.

`LIBERO_C6_proposal_recovery = NOT_TESTED_REPRESENTATION_GATE_FAILURE`.
