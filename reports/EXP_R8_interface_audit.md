# EXP_R8 interface audit

R8 changes only the train/development candidate-selection rule. It reuses the bounded four-step residual candidates from R7 and leaves all frozen model interfaces unchanged.
