# Next experiment from EXP_R6

EXP_R6 is **NOT_SUPPORTED**. If it fails, EXP_R7 should add a state-conditioned residual correction with a bounded terminal target, while preserving the smooth proposal and frozen F1/F2. If it succeeds, remove oracle endpoint selection and test target-set planning.
