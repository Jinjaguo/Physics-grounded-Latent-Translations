# Next experiment from EXP_R12

R12 tested nearest, density, margin, and ensemble target-set terminal capture under the R11 surrogate plant. It selected `proposal_ensemble` with claim NOT_SUPPORTED. The next experiment should test causal subgoal completion confidence from observable robot/latent history while retaining oracle F3 for F2, because endpoint choice is not yet a robust solution.
