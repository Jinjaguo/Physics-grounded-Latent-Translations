# EXP_R7 interface audit

EXP_R7 only adds a bounded endpoint residual to the existing four-step proposal. The correction is computed from the train-only target region and is distributed over four waypoints by a fixed schedule. Representation, decoder, F1 and F2 remain frozen.
