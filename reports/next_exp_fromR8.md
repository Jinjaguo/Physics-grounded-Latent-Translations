# Next experiment from EXP_R8

EXP_R8 is **SUPPORTED**. If it fails, EXP_R9 should learn a state-conditioned confidence/residual adapter and compare it with the fixed feasible selector. If it succeeds, remove oracle endpoint selection and test target-set planning.
