# Next experiment from EXP_R27

EXP_R27 established only an interface gate for oracle-F3 completion, calibration, and two-step long-horizon integration; it did not open held-out control. The immediate next step is: keep F3 oracle and collect valid closed-loop transitions before integrating learned switching. Keep representation, decoder, F1, old F2, R8 and all prior negative results frozen. EXP_R28 should test a new causal interface or explicitly preserve the gate failure.
