# Next experiment from EXP_R32

EXP_R32 established only an interface gate for long-horizon ordered task composition and atomic-action protection; it did not open held-out control. The immediate next step is: restore simulator snapshots or run prospective CALVIN episodes with controller state recorded. Keep representation, decoder, F1, old F2, R8 and all prior negative results frozen. EXP_R33 should test a new causal interface or explicitly preserve the gate failure.
