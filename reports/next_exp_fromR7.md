# Next experiment from EXP_R7

EXP_R7 is **NOT_SUPPORTED**. If it fails, EXP_R8 should condition a residual adapter on source state, target language, and proposal confidence, and compare it with a conservative closed-loop correction. If it succeeds, remove oracle endpoint selection and test target-set planning.
