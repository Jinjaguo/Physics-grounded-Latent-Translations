# EXP_R6 data audit

The episode-disjoint EXP_R3 inventory is reused unchanged (864 cases). Proposal fitting uses train cases, alpha selection uses development cases, and held-out paths are opened once after selection.
