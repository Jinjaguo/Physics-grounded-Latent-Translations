# Wave40 semantic/execution split transport

Wave39 showed that a single latent force can be semantically pointed at the
new instruction without becoming an executable action.  Wave40 separates the
force into semantic and execution branches with independent low-rank bases and
learned magnitude calibration.  Compare separate, shared-gate, calibrated,
and cross-coupled branches, q=2/4/8, PCA/random bases, and action/semantic loss
weights.  No future action or latent is an input.  Continue unless success or
Wave78.
